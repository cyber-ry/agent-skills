# Fast deterministic checks only. Skill-structure validation fetches every
# external link, so a flaky network would fail a good commit; CI runs it.
npm run validate:plugins
npm run validate:eval-baselines
